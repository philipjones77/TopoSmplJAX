# Contributing

1. Create a feature branch.
2. Add or update tests for behavior changes.
3. Run `python -m pytest tests -q` before submitting.
4. Keep APIs JAX-transform compatible (`jit`, `vmap`, `grad`).
