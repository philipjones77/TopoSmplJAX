# Tests

This folder is the repository-wide automated test namespace.

Governed structure:

- `common/`: tests for shared repository behavior
- `topo/`: TopoJAX-owned tests
- `smpl/`: smplJAX-owned tests

Place new tests in the backend or shared subtree that owns the behavior under test.
