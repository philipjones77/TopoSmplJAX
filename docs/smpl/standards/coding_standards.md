# Coding Standards (Version 1.0.0)

- Prefer explicit array dtypes.
- Keep public API signatures stable.
- Avoid hidden runtime side effects in hot paths.
- Keep optional dependencies (`viser`) isolated and lazy-loaded.
- Include tests for each behavior change.
