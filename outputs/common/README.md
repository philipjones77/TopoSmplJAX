# Common Outputs

Place retained exported artifacts here when they belong to shared repository workflows rather than a single backend.

Examples:

- shared conversion outputs
- combined reports
- cross-backend validation artifacts
