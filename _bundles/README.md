# Bundles

This folder stores generated source bundles and similar packaging artifacts.

Repository packaging utilities should write here by default.

For the standard source snapshot, use:

```bash
python tools/zip_repo.py
```
